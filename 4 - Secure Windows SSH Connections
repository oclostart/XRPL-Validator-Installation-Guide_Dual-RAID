# SSH Set up
sudo apt update
sudo apt install -y ufw
sudo ufw allow 51235/tcp
# If not traveling and connecting from local network pcs
sudo ufw allow from x.x.x.x to any port 22
# If traveling and connecting from different IPs
sudo ufw allow 22/tcp 
